function insertionSort(tab) {
    for (let i = 1; i < tab.length; i++) {
        let key = tab[i];
        let j = i - 1;  
        while (j >= 0 && tab[j] > key) {
            tab[j + 1] = tab[j];
            j--;
        }
        tab[j + 1] = key;
    }
    return tab;
}
